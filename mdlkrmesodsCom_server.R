# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
mdlkrmesodsServer::krmesstockodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesrouteodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesproductionwarehousinodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesproductionreturnodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesproductionrequisitionodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesproductionorderodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesproductionfeedodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesprocesstransferodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesprocessreportodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesprocessplanningodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesprocessodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesmaterialodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesflexvaluesodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesempodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesdeptodsServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsServer::krmesbomodsServer(input = input,output = output,session = session,dms_token = dms_token)



  

